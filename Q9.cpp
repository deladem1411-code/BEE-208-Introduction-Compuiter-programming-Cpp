#include <iostream>
using namespace std;

int main() {
    int x = 5;   // assignment

    cout << "Assignment operator (=) assigns values." << endl;
    cout << "Equality operator (==) compares values." << endl;

    if (x == 5)
        cout << "x is equal to 5";

    return 0;
}